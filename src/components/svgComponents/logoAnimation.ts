
const animateBubbles = async (bubbleQueue) => {
  // animate each bubble upwards along y axis
  // time emergence of bubbles so that once the bubble travels specified distance, it is added back to the queue
  bubbleQueue.forEach(bubble => {
    return new Promise(resolve => {
      
    })
  })
}